{
  "name": "supaflare-monorepo",
  "private": true,
  "author": "ian <licitdev@gmail.com>",
  "license": "MIT",
  "workspaces": [
    "./app",
    "./worker"
  ],
  "devDependencies": {
    "lerna": "^5.1.6",
    "patch-package": "^6.4.7"
  },
  "scripts": {
  "postinstall": "if [ ! -x node_modules/.bin/patch-package ] ; then print(hello) npm install patch-package --save-dev; fi && patch-package"
  "build": "wrangler build"
}
}
